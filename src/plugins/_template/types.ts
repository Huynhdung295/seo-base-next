// ─────────────────────────────────────────────────────────────
// 📝 PLUGIN TEMPLATE — Types
// ─────────────────────────────────────────────────────────────
// Define your plugin's configuration interface here.
// This is what consumers pass when using the plugin.
// ─────────────────────────────────────────────────────────────

/**
 * Configuration for the plugin.
 * 
 * @example
 * const config: MyPluginConfig = {
 *   // ... your config options
 * };
 */
export interface MyPluginConfig {
  // Define your config options here
  // Example:
  // title: string;
  // variant?: "default" | "compact";
}
